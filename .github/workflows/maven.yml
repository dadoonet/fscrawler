name: Build and Deploy the main branch

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      skip_docker:
        description: Skip Docker Hub login and image push
        type: boolean
        default: false
      skip_github:
        description: Skip GitHub SNAPSHOT pre-release
        type: boolean
        default: false
  pull_request:
    types: [labeled]

jobs:
  build-and-deploy:
    if: github.event_name != 'pull_request' || github.event.label.name == 'test-main-workflow'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      SKIP_DOCKER: ${{ github.event_name == 'pull_request' || inputs.skip_docker == 'true' }}
      SKIP_GITHUB: ${{ github.event_name == 'pull_request' || inputs.skip_github == 'true' }}
    steps:
      - uses: actions/checkout@v7
      - name: Fetch origin/main for Spotless ratchet
        run: git fetch origin main --depth=1
      - name: Set up JDK 21
        uses: actions/setup-java@v6
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
      - name: Cache Docker images
        if: env.SKIP_DOCKER != 'true'
        uses: AndreKurait/docker-cache@0.7.0
        with:
          key: fscrawler-docker-cache-${{ runner.os }}-${{ hashFiles('pom.xml') }}
        continue-on-error: true
      - name: Login to Docker Hub
        if: env.SKIP_DOCKER != 'true'
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Create Maven settings.xml
        if: env.SKIP_DOCKER != 'true'
        uses: s4u/maven-settings-action@v4.0.0
        with:
          githubServer: false
          servers: '[{"id": "docker.io", "username": "${{ secrets.DOCKER_USERNAME }}", "password": "${{ secrets.DOCKER_PASSWORD }}"}]'
      - name: Build SNAPSHOT ZIP
        if: env.SKIP_DOCKER == 'true'
        run: mvn --batch-mode package -DskipTests -Dossindex.skip -Ddocker.skip
      - name: Deploy Docker images
        if: env.SKIP_DOCKER != 'true'
        run: mvn --batch-mode deploy -DskipTests -Dossindex.skip
      - name: Publish SNAPSHOT ZIP as GitHub pre-release
        if: env.SKIP_GITHUB != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 scripts/publish_snapshot_prerelease.py
