name: "test-static-metadata"

fs:
  url: "/path/to/docs"
  update_rate: "5m"

tags:
  meta_filename: ".meta.yml"
  staticTags:
    external:
      hostname: "server001"
      environment: "production"
    custom:
      category: "documents"
      source: "filesystem"

elasticsearch:
  index: "test_static_docs"